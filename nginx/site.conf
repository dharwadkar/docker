server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Custom logging to stdout and stderr
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
}
